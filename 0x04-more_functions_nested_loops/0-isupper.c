#include "main.h"
/**
 * _isupper - check to see if a letter is in uppercase
 *@c: The number that needs to be evaluated
 */
int _isupper(int c)
{
        if (c >= 65 && c <= 90)
        {
        return (1);
        }
        return (0);
}
