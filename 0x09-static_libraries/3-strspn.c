#include <stdio.h> 
#include <ctype.h> 
#include <stdlib.h>
#include <string.h>
/**
* _strspn - Computes the length of the initial segment of a string that consists
* of only the characters in accept
*/
unsigned int _strspn(char *s, char *accept)
{
	/* Implement this function */
	return (0);
}
