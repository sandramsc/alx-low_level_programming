#include <stdio.h> 
#include <ctype.h> 
#include <stdlib.h>
#include <string.h>
/**
* _strpbrk - Finds the first occurrence in a string of any of a set of characters
* of only the characters in accept
*/
char *_strpbrk(char *s, char *accept)
{
	/* Implement this function */
	return (NULL);
}
