#include <stdio.h> 
#include <ctype.h> 
#include <stdlib.h>
#include <string.h>
/**
* _strstr - Finds the first occurrence of a substring in a string
* of only the characters in accept
*/
char *_strstr(char *haystack, char *needle)
{
	/* Implement this function */
	return (NULL);
}
