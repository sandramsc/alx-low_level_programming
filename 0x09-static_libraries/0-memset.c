#include <stdio.h> 
#include <ctype.h> 
#include <stdlib.h>
#include <string.h>
/**
* _memset - Sets the first n bytes of a string to a value
*/
char *_memset(char *s, char b, unsigned int n)
{
	/* Implement this function */
	return (s);
}
